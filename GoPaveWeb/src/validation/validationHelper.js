export const minimumValueString = (minimumValue) => (
  `Min. value is ${minimumValue}. Enter a valid value`
)

export const maximumValueString = (maximumValue) => (
  `Max. value is ${maximumValue}. Enter a valid value`
)

export const maximumBSTThicknessValueString = (maximumValue) => (
    `Chip seals are thin (<0.5 in), non-structural layers. For added structure, use HMA. Max. value is ${maximumValue}. Enter a valid value`
)